## New version

- New `ensemble_models()` function for the Open-Meteo Ensemble Models API (contributed by @jacobmmears)
- `air_quality()` now supports the `current` argument and validates input types
- Added `URL` and `BugReports` to DESCRIPTION
- Fixed `weather_variables()` after Open-Meteo moved their OpenAPI specs

## R CMD check results

0 errors ✔ | 0 warnings ✔ | 0 notes ✔

(Local `--as-cran` run produced one NOTE about being unable to verify the current time, which is a network/environment artefact unrelated to the package.)

## Reverse dependencies

None on CRAN.
